import styled from "styled-components";

export const StyledTimeBar = styled.time`
	font-size: 16px;
	letter-spacing: 0.3px;
	margin-bottom: 10px;
	align-self: flex-end;
	color: var(--secondary-color);
`;